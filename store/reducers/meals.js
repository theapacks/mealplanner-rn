import { MEALS } from '../../data/dummy-data';

const initialState = {
    meals: MEALS,
    filters: MEALS,
    favourites: []
}
const mealsReducer = (state, action) => {
    return state;
}

export default mealsReducer;